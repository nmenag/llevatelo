<ul class="nav navbar-nav navbar-right">
  <% if user_signed_in? %>
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
        <i class="fa fa-user" aria-hidden="true"></i> Mi Perfil <span class="caret"></span>
      </a>
      <ul class="dropdown-menu">
        <li>
          <%= link_to '<i class="fa fa-hand-pointer-o"></i> Mis Publicaciones'.html_safe,  my_articles_path %>
        </li>
        <li>
          <%= link_to '<i class="fa fa-heart"></i> Mis Favoritos'.html_safe, my_favorites_path %>
        </li>

        <li>
          <%= link_to '<i class="fa fa-id-badge"></i> Editar Perfil'.html_safe, edit_user_registration_path %>
        </li>
        <li role="separator" class="divider"></li>
        <li>
          <%= link_to '<i class="fa fa-sign-out"></i> Salir'.html_safe, destroy_user_session_path, method: :delete %>
        </li>
      </ul>
    </li>
  <% else %>
    <li>
      <%= link_to 'Ingresar/Registrarse', new_user_session_path, class: 'btn navbar-btn btn-ghost btn-sign-in' %>
    </li>
  <% end %>

  <li  class="page-scroll sign-in">
    <%= link_to '<i class="glyphicon glyphicon-plus"></i> Publicar artículo'.html_safe, new_article_path, class: 'btn navbar-btn btn-ghost btn-started' %>
  </li>
</ul>