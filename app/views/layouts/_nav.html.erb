<!-- Navigation -->
<nav class="navbar navbar-fixed-top navbar-custom">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header page-scroll">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span> Menu <i class="fa fa-bars"></i>
      </button>
      <%= link_to 'Llevatelo.co', root_path, class: "navbar-brand"  %>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to 'Acerca de', about_pages_path %>
        </li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.email.split('@')[0] %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to '<i class="fa fa-hand-pointer-o"></i> Mis Publicaciones'.html_safe,  my_articles_path %>
              </li>
              <li>
                <%= link_to '<i class="fa fa-heart"></i> Mis Favoritos'.html_safe,  my_articles_path %>
              </li>

              <li>
                <%= link_to '<i class="fa fa-id-badge"></i> Editar Perfil'.html_safe, edit_user_registration_path %>
              </li>
              <li role="separator" class="divider"></li>
              <li>
                <%= link_to '<i class="fa fa-sign-out"></i> Salir'.html_safe, destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>

          <li>
            <%= link_to '<i class="glyphicon glyphicon-plus"></i> Nuevo producto'.html_safe, new_article_path %>
          </li>
        <% else %>
          <li class="page-scroll sign-in">
            <%= link_to '<i class="fa fa-sign-in"></i> Login'.html_safe, new_user_session_path  %>
          </li>
        <% end %>

      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
   <!-- /.container-fluid -->
</nav>
